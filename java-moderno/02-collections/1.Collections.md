# Java Moderno – Resumo 

## 1. Collections 

Collections existem para guardar, organizar e acessar dados na memória. Cada uma faz isso de um jeito diferente

### List - quando a ordem importa

É uma coleção:
- ordenada
- permite elementos repetidos
- acessada por índice

Pense como: uma lista numerada (0, 1, 2, 3...)

🔹Implementações mais usadas  
- ArrayList
- LinkedList

Primeiro, inicializamos a variável nomes do tipo List e instanciamos como uma ArrayList vazia.
```java
List<String> nomes = new ArrayList<>();
```

Obs.: List é uma interface, ou seja, quem for do tipo List precisa ter os métodos contidos na classe, como add().
Não tem implementação, apenas regras. É um contrato.
ArrayList é uma classe concreta que implementa a interface List.
no exemplo acima: nomes é do tipo List, mas a instância criada é de ArrayList.
🔹Operações essenciais 
```java
nomes.add("Ana");
nomes.add("João");
nomes.add("Ana"); // permitido

nomes.get(0);      // "Ana"
nomes.size();      // 3
nomes.contains("João"); // true
nomes.remove("Ana");    // remove a primeira ocorrência

```

🔹Percorrer uma List

```java
for (String nome : nomes){
    System.out.println(nome);
        }
```

Utilizando índice:

```java
for (int i = 0; i < nomes.size(); i++){
    System.out.println(nomes.get(i));
        }
```
### Set - quando NÃO pode repetir

É uma coleção: 
- não permite duplicados
- não garante ordem (na maioria dos casos)
- usada para unicidade   

🔹Implementações mais usadas  
- HashSet -> mais usado
- LinkedHashSet -> mantém ordem de inserção
- TreeSet -> mantém ordenado

```java
Set<String> emails = new HashSet<>();
```
🔹Operações Essenciais

```java
emails.add("a@email.com");
emails.add("b@email.com");
emails.add("a@email.com"); // ignorado

emails.size(); // 2
emails.contains("b@email.com"); // true
emails.remove("a@email.com");

```

🔹Percorrer um Set

```java
for (String email : emails) {
        System.out.println(email);
}

```